# <common-模块名>

<!-- 模板：API 字典（reference/api）。复制本文件到 docs/reference/api/<common-模块名>.md 后填写，交付前删注释。
     适用：一个 common 模块一篇的查表文档（宽松件镜像）。规范权威在 specs/current/modules/ 同名法卷，本文只描述事实、一律指针。 -->

## 1. 定位与边界

<这个模块一句话是什么；管到哪、不管哪。>

### 包结构

<!-- 可选：顶层包一览。结构以源码为准，此处只画读者需要的骨架，不手抄全树（归属法禁二手结构树）。 -->

## 2. 核心能力

<!-- 每个能力一个 ### 小节：一句话职责 + 关键签名/表的描述。代码标识符原样引用（C3 核验符号存在）；涉对账表（如异常映射）行列结构不动，由闸核对。 -->

### <能力一>

<一句话 + 关键表或签名清单。>

### <能力二>

## 3. 使用方式

<引入 + 最小可用配置的描述；逐步教程在 tutorials/，规范场景在法卷——此处只放指针。>

## 4. 依赖关系

<引入本模块即带入什么、opt-in 什么、要求的上游版本。与法卷 §依赖同源，冲突以代码与法卷为准。>

## 5. 设计原则

<3-6 条，各一两句；论证细节指向 explanation/，不展开。>

## 6. 设计决策（已迁出）

<!-- 固定节，只放指针：决策与理由不在本文复述。论证 → explanation/<篇>；条款 → specs/<卷 §节>。 -->

## 7. 职责边界与技术债

<如实写：本模块刻意不做什么、已知欠账与限制。技术债逐条，不遮掩。>

---

**交稿自查**：

- [ ] 七节齐全、节序与节号未改（外部大量按 "api/<篇> §N" 引用）
- [ ] 只描述不立法：出现"必须/禁止"句 = 移到法卷，此处换指针
- [ ] 代码标识符与磁盘符号一致（C3）
- [ ] 对账型表格（如有）行列结构与闸的谓词一致
- [ ] 无 `ADR-` + 四位数字
