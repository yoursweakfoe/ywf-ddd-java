# <名> —— 从零<完成什么>

<!-- 模板：教程（tutorials）。复制本文件到 docs/tutorials/<名>.md 后填写，交付前删注释。
     适用：线性操作手册，读者零基础、要动手、目标是第一次成功。教程是真实例操作手册：业务词豁免（C4 不扫本区）。 -->

<开头两三行：跑完这篇你会得到什么、大概花多久、坏了去哪求助。>

## 0. 前置

<!-- 逐项可验证：环境版本、依赖服务、凭据。给检查命令，不只给"要有 java"。 -->

- <依赖一>（<版本要求>）——检查：`<命令>`
- <依赖二>

## 路线 A｜一条命令跑通验收（推荐第一步）

<最短路径：一条命令 + 预期输出（贴真实样例，别手搓理想输出）。失败最常见原因一句话。>

```bash
<命令>
```

预期：

```
<真实输出片段>
```

## 路线 B｜手动分步

<!-- 每步一节：### n. 动作。命令块 + 预期输出块 + 为什么这一步（一行）。步骤编号连续，读者能停在任一步看到当前该干嘛。 -->

### 1. <动作>

```bash
<命令>
```

<输出与说明>

### 2. <动作>

## 体验失败通道（可选）

<!-- 故意制造 2-3 种典型错误（非法输入、状态违例…）让读者看见真实的错误形状：状态码 + 响应体样例。这节是教程独有的价值——API 文档不会告诉你错了长什么样。 -->

## 实现状态表

<!-- 教程不演示但文档其他部分存在的功能，如实登记，防止读者以为文档吹牛。 -->

| 项 | 状态 | 说明 |
|---|---|---|

## 下一步

<出口指针：想深入 → 解读篇；想做任务 → 设计卡；想查事实 → 字典。一行一个去处。>

---

**交稿自查**：

- [ ] 每条命令复制即可执行；预期输出是真实贴回来的
- [ ] 从零读者不需要任何未写明的前置知识
- [ ] 全文只有一个 H1
- [ ] 代码变了 → 本文当天重跑核对（教程与代码不符 = 文档 bug）
- [ ] 已登记 docs/README
