# 实现清单（按依赖序，[P]=可并行）
- [ ] <任务粒度 = 一次可验收的动作；每条尾巴指向 delta 的 Requirement 名>
- [ ] 测试：GIVEN/WHEN/THEN 场景 → 断言落点（单测/集成，参照 how-to/testing.md）
- [ ] 归档折叠：`spec-delta.md` 合入 `current/<agg>.md`，本目录移入 `archive/`——**文档同步义务唯一时点**
