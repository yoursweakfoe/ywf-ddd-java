# 用法规范法卷：<主题>（框架法 · 严格件）

<!-- 模板：模式法卷·通式。复制本文件到 current/patterns/<主题>.md 后逐节填写，交付前删除全部 HTML 注释。
     适用：一条链、一个模式的横切规范。不适用：清单先行→blueprint.md；禁令为主→prohibitions.md；元法规→attribution.md。 -->

> **身份**：本卷是<主题>统一用法的唯一权威，条款与全套规范代码形状都住本卷，全仓其他位置不得复写这些形状。代码违反本卷就修代码；修订本卷只能走 `../../changes/` 立案程序。docs 同题篇 `../../../docs/how-to/<卡名>.md` 是设计卡，只讲选型与边界，零形状代码；两处冲突时以本卷为准。
> **机器对账**：C1 检查 `{agg}` 实例化、C3 检查符号、C4 检查教学中立，三道闸扫本卷。<教例家族声明，例：教例家族 Reservation 是虚构教例，sample 未实现；真实例名称只准出现在带「真实例」标记的指针位。>

## §1 条款

<!-- 一行一条，编号 = 本卷前缀-序号（如 WC-1）。每条 SHALL 必须可机械化执行；讲道理、给语感的句子放 docs，不放这里。取证不许空：真实 文件:行、测试名、javadoc 节名，或缺证据时用 <!-- 待 ../../changes/ 补全 --> 留缺口。 -->

| # | SHALL | 取证 | 背书 |
|---|-------|------|------|
| XX-1 | <一句强制句> | <实现文件:行 / 测试类名 / javadoc> | <ArchUnit R## / C# 闸 / 评审项> |
| XX-2 | <…> | <…> | <…> |

## §2 规范形状（统一用法唯一样本）

<!-- 首段写教例设定：虚构家族 + 走查案例一句话 + 首现标注「虚构教例，sample 未实现」。之后按落地顺序逐件走查：### 2.1 固定为调用链路/全景图，中间各节一层一段，末节固定为教例文件清单。代码块是全仓唯一样本，教学用 {agg} 通配或虚构家族名，真实类名只进「真实例」标注位。 -->

### 2.1 调用链路

### 2.2 <层/件走查一>

### 2.3 <层/件走查二>

### 2.4 教例文件清单

| 件 | 位置 | 职责一句话 |
|---|------|-----------|

## §3 生效登记

<!-- 逐条登记：✅ 生效（写落点）/ ⛔ 未落地（写模板位置与缺口原因）。未生效必须写明，不允许装死。 -->

| 条款/环节 | 状态 | 位置 |
|-----------|------|------|

---

**交稿自查**：

- [ ] 节序 §1 条款 → §2 形状 → §3 生效登记，未增删主干节
- [ ] 每条条款可机械化；不可机械化的句子已移到 docs
- [ ] 取证列全部为真实存在的 文件:行/测试名（或留缺口注释）
- [ ] 形状代码用虚构教例；真实例仅在标注位；`{agg}` 路径可被 C1 实例化核验
- [ ] 全文无 `ADR-` + 四位数字；计数宣称与磁盘一致
- [ ] docs 同题设计卡存在且首行互指
