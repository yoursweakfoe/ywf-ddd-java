# 用法规范法卷：<对象>构建宪（框架法 · 严格件）

<!-- 模板：模式法卷·构建宪式。复制本文件到 current/patterns/<主题>.md 后填写，交付前删注释。
     适用：以"文件槽位清单"为核心资产的卷——新对象建齐哪几个文件、每件什么形状。通式卷够用就别用这份。 -->

> **身份**：本卷规定<对象>由哪些文件构成、每个文件什么形状。完整清单共 **NN 个文件**：<拆分口径一句话>。修改本卷必须走 `../../changes/` 立案。docs 同题篇 `../../../docs/how-to/<卡名>.md` 是设计卡，只讲选型与边界，零形状代码；逐件教学走查全部收在本卷 §4。内容冲突时以本卷为准。
> **机器对账**：C1 把下列 `{agg}` 位以真实聚合代入核验；C3/C4 扫本卷。<教例家族声明>。
> **注意**：NN 计数宣称会被 C2 与磁盘核对，改清单必须同步改数。

## §1 文件清单（①-㊞ 为全局槽位号）

<!-- 主干资产：目录树代码块，每个文件右侧标槽位号与一句话职责。圈号连续编号，后续节按此号引用。 -->

```
<module>/
├── <层一>/
│   └── <file>.java     ← ① <一句话职责>
└── ...
```

## §2 形状条款

<!-- 同通式卷 §1：可机械化条款 + 取证 + 背书。清单本身的完成度也在这里立法（如"缺任一件即违反本卷"）。 -->

| # | SHALL | 取证 | 背书 |
|---|-------|------|------|
| BP-1 | <…> | <…> | <…> |

## §3 验收单（交付闸，逐条可机械化）

<!-- 建完一个对象后逐条勾的清单；这里是"怎么算建对了"，不是条款复述，勾选项写成可执行判断句。 -->

- [ ] <检查项一，如：编译 + ArchUnit 全绿>
- [ ] <检查项二>

## §4 全套规范形状（①-㊞ 逐件走查）

<!-- 一件一节，标题固定格式 "### 4.<槽位号> <层> — <件名>"，顺序 = 落地顺序（依赖倒序或施工序）。节内：位置 + 职责一段 + 代码样本。 -->

### 4.① <层> — <件名>

### 4.② <…>

## §5 服务骨架通式（本卷的 <更大单元> 级扩展）

<!-- 可选：对象放进完整部署单元时，外围还有哪些固定件（配置/启动/资源文件），指针到对应法卷，不重复形状。 -->

## §6 生效登记

| 条款/环节 | 状态 | 位置 |
|-----------|------|------|

---

**交稿自查**：

- [ ] §1 槽位号连续无跳号，§4 逐件走查与 §1 清单 1:1 对位
- [ ] NN 文件计数 = §1 清单实际行数（C2 核对）
- [ ] 每条条款可机械化；验收单每条可独立勾验
- [ ] `{agg}` 位全部可被真实聚合实例化核验
- [ ] 无 `ADR-` + 四位数字；教例家族首现标注
