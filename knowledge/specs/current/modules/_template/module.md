# 模块用法法卷：<common-模块名>（框架法 · 严格件）

<!-- 模板：模块法卷。复制本文件到 current/modules/<模块名>.md（与模块同名）后填写，交付前删注释。
     适用：一个 common 模块的用法规范。描述性镜像在 docs/reference/api/（用 api.md 模板），本卷只管规范。 -->

> **身份**：本卷是 <common-模块名> 用法规范与规范代码形状的唯一权威（严格件）。消费代码必须遵循本卷；违反本卷就修代码。修改本卷只能走 `../../changes/` 程序。docs 同题节（`reference/api/<模块名>.md`）是宽松件，只承载语感与指针；两者冲突时以本卷为准。
> **机器对账**：本卷在 check-docs 扫描面内。C1 校验 `{agg}` 模板实例化，C3 校验符号解析，C4 校验教学中立。<涉对账表加一句：例 异常→HTTP 映射表由 C5 对账。>

---

```xml
<dependency>
    <groupId>com.yoursweakfoe</groupId>
    <artifactId><common-模块名></artifactId>
</dependency>
```

<引入方式一句话，例：引入即生效：<机制>。/ opt-in：<用到才声明什么>。>

## §1 定位与边界

<这个模块管什么、不管什么，3-5 行。不管的部分指明归哪个模块。>

## §2 使用场景

<!-- 每个场景一节：标题写动作。正文 = 场景一句话 + 可抄代码块 + 注意事项。场景覆盖真实用法全集，宁多勿漏；示例用 {agg}/虚构家族，真实类名只进「真实例」标注位。 -->

### 场景 1：<动作>

<一句话>

```java
<代码>
```

### 场景 N：<动作>

## §3 模块条款（可选）

<!-- 有跨场景的强制句才设本节；纯用法演示卷可整节删掉（删节 = 形状变化，走 changes/）。编号 = 模块前缀-n（如 TR-1），同通式卷条款表格式。 -->

| # | SHALL | 取证 | 背书 |
|---|-------|------|------|

## §4 全链路条款与形状（可选）

<!-- 仅横切全仓的模块设（如 exception 的 EV 系）：条款表 + 全链路形状走查，格式同通式卷 §1/§2。 -->

## §5 生效登记

| 条款/场景 | 状态 | 位置 |
|-----------|------|------|

---

**交稿自查**：

- [ ] `##` 级节号齐全（§1 起，可选节整节存在或整节没有，不留空壳）
- [ ] 每个代码块可独立抄用；`{agg}` 位可被 C1 实例化核验
- [ ] 每条 SHALL 有真实取证；无证据用 <!-- 待 ../../changes/ 补全 --> 留缺口
- [ ] api 宽松件已同步或登记为待同步（docs 同题=地图）
- [ ] 无 `ADR-` + 四位数字；「引入即生效/零配置」等事实与代码本体核对
