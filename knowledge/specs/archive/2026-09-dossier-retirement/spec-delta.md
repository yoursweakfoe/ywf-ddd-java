# 对 current/patterns/attribution-law.md 的增删改
> 只描述变化量，不描述全世界（delta 教义）。每条带 SHALL + 场景。本案为**纯宣称文本 + 工具法**案（不触 Java 行为）：条文取证源 = 被改条款现行文本；工具条款取证源 = check-docs.ps1 清册演练运行输出（→ tasks 6）。行文法：目录名主词，旧指代词括注。镜像区 `sample-application/specs/` 依宪章「同文援引」同受本制。项目主裁定基调（2026-09-09）：**彻底抹除**——沉淀完整是删除的前提，删除干净是沉淀的证明。

## ADDED Requirements

### Requirement: §1「卷宗清册制」注（新段，紧随「三类件分家」注之后）
`specs/archive/`（含镜像区 archive）与 `decisions/` 两个本体存放区（合称**两册**）是**可清册存储**：于发布节点或项目主宣告时，经清册 bill **彻底抹除**——本体整袋删除（禁拆件、禁在位改、禁择留）+ README 账行同裁（README 常驻的是章程，不是账目）+ 活面**全部具体标识符净空**（`ADR-NNNN` 编号、date-slug 案名、解读篇页脚快照账行——改写为无编号散文或整行删除，行灭理存）。**自足判据**：抹除前，每案的**前因后果**（立因、取舍、被拒方案之拒因、生效边界）须已完整住进 `docs/explanation/` 或 current/ 条文——判据一句话：**一个从未见过卷宗的读者，能从解读架复述该案全部因果**。未过判据 = 先补沉淀再清册，缺节不得批。存活件仅两项：**解读架的因果全文 + current/ 的条文现行文本**。git 历史**不是**存活件也不是任何层的存在依赖（**法不考古**：知识的权威面永远在工作树）。**清册即改元**：两册归零、编号历元重启（新案自 `ADR-0001` 起，历元内唯一，跨历元同号不救济）；执行清册的 bill 自身亦是案卷，下期清册随区归零（**仪式产物不豁免于仪式**）。机器执法：C6 清册臂（区空 ⇒ 活面残留该期具体标识符即红；区不空 ⇒ 本体整删仅出自清册 bill 的 diff 放行）；在位涂改仍由冻结臂零容忍。
（源：本 delta 折叠后由 §1/§2/§4 改文自证；清册臂演练取证 → tasks 6）
#### Scenario: 合规彻底清册
- GIVEN 清册 bill 附逐案再核表且全案过自足判据（因果俱在解读架）
- WHEN 本体整袋删除、账行同裁、活面标识符净空
- THEN 两册空壳（仅章程），全活面 grep `ADR-\d{4}`（占位形除外）与 date-slug 形 = 0，C6 两臂绿
#### Scenario: 沉淀不全，删除被拒
- GIVEN 某案「被拒方案之拒因」仅存于其 proposal，解读架无载
- WHEN 清册再核
- THEN 该案不得入清册名单——先补解读篇（或 theory-map 事务账），判据过了才许抹
#### Scenario: 改元重启
- GIVEN 一期清册完成，两册空
- WHEN 下游/本仓立新决策
- THEN 编号从 `ADR-0001` 起，册目录以空章程交付，如新仓启动
#### Scenario: 清册臂抓残痕
- GIVEN decisions/ 已空，但某解读篇页脚仍写 `ADR-0007（决策快照）`
- WHEN check-docs 运行
- THEN C6 清册臂红——空册配净面，残号即漏抹

### Requirement: §4 强制同步表「卷宗清册执行」行
清册 SHALL 由独立 bill 承载并执行七步仪式：① **逐案再核表**（四栏：立因/取舍/被拒方案及拒因/生效边界 → 去向 = 解读篇某节某段，或判「纯史件，随灭」；**表随 bill 递交，缺案缺栏则批准门不得通过**）；② **补沉淀**——再核发现的因果缺口，同 bill 内先复写进解读架（⑦⁺ 的追溯执行）；③ **整袋删除**——whole-directory/whole-file，禁拆件；④ **净账**——README 账行同裁，两区 README 回到空壳章程态；⑤ **净面**——全活面改写：`ADR-NNNN`/slug 具体标识符删除或转无编号散文（含页脚快照账行整行删、法卷「某某案 立」生效注转年份散文），C1 验零幽灵；⑥ **连带**——计数宣称、glossary、docs/README、theory-map 账本同步；⑦ 七闸绿（C6 清册臂 + 空册自洽 + `check-diagrams` + `mvn compile`）。
（源：check-docs.ps1 C6 块实施后运行证据 → tasks 6；本行文本）
#### Scenario: 发布节点例行清册
- GIVEN 项目主宣布发布节点，两册在库
- WHEN 七步执行完毕
- THEN 交付态 = 空两册 + 满解读架，任何读者无从得知（也无须得知）上一世有几案

## MODIFIED Requirements

### Requirement: §1「三类件分家」注   <!-- 折叠时整段替换 current/ 同名段 -->
**三类件分家**（旧称「案卷与判例分家」；2026-09 `2026-09-archive-dossier-boundary` 立，2026-09 `2026-09-adr-argument-sedimentation` 补沉淀分层，2026-09 `2026-09-dossier-retirement` 补清册制）：`specs/changes/`（审议中）与 `specs/archive/`（折叠后整袋迁入）里的三件套 = **立法过程件**（旧称「案卷」）——问题陈述、条款变化量（delta）、表决行、施工勾验；折叠即使命终、封存只进**不改在位**，到期整册抹除（→「卷宗清册制」注）。`decisions/`（`ADR-NNNN` 本体）= **决策事件件**（旧称「判例卷宗」）——决策本身、当时的 context / 取舍 / consequences（思考快照）、Confirmation 授权收据；全局编号、append-only、跨时间被引（新案先查旧案、supersede 靠旧案快照活着）；本体同属**可清册存储**，历元终了随区归零（→ 彼注）。`docs/explanation/` = **知识沉淀件**——**论证的现行版**（"这部法今天为什么成立"）的 canonical 居所，折叠时刻（⑦⁺）自 ADR 强制复写进来（→ §4「案卷折叠时（⑦⁺）」行），页脚回指 `ADR-NNN（决策快照）` 供活册期内溯源，清册时刻随灭。卷宗与解读的论证重叠**不是双写病灶，是源与流**：卷宗=冻结收据（回答「当时怎么定的、谁批的」），解读=活的现行（回答「今天为什么如此」），时效语义不同、执法闸各异（C6 护前者、§4 同步护后者）；流必须**先于源灭而独立存在**（自足判据）。两栖病灶（changes/archive 的 proposal 裁决表与 ADR Decision Outcome 近同文双写）的裁决一句话，今升级为**三归一清**：**同一事实，过程归 `changes|archive/`、事件与当时思考归 `decisions/`、沉淀道理（现行版，含前因后果）归 `docs/explanation/`——前两者的本体到期彻底抹除，唯沉淀件永续**。
（源：attribution-law.md:19 现文，本 delta 全文替换）

### Requirement: §2 表「架构决策（事件与当时思考）」行   <!-- 整行替换 -->
| 架构决策（事件与当时思考） | 冻结在位，到期整册抹除 → §1 清册制注 | `decisions/`（`ADR-NNNN` 全局编号，Confirmation 必填；历元内唯一，清册改元归零） | 活册期内：`ADR-NNN` 限定名引用，**一律纯文本禁超链**；`changes/`+`archive/` 内涉已立 ADR 之决策只准表决行 + 指针，禁在过程件复写论证正文（正文的**现行沉淀版且含前因后果**住 `docs/explanation/`——源流非副本，见 §1）；清册期后：标识符净空，解读篇自足 | C6 冻结臂 + 清册臂 + Confirmation 必填 |
（源：attribution-law.md:31 现文，本 delta 全文替换）

### Requirement: §2 表「立法/裁决过程事实」行   <!-- 整行替换 -->
| 立法/裁决过程事实 | 随案卷，到期整册抹除 → §1 清册制注 | `specs/changes|archive/` 案卷本体（提案问题单、delta、表决行、tasks 勾验） | 判例卷宗与 docs 只准指针；折叠前过瘦身闸（§4 行）；清册前过再核闸（§4 清册行，自足判据） | ddd-review 人肉抽查 + C6 清册臂（空册自洽） |
（源：attribution-law.md:32 现文，本 delta 全文替换）

# 对 specs/README.md 的增删改（法区宪章）

### Requirement: 目录表 `archive/` 行   <!-- 整行替换 -->
| `archive/` | **已归档案卷**：折叠完成的案卷（date-slug 命名）——过程件，决策论证的家在 `decisions/` 与 `docs/explanation/`（归属法 §1 分家注）；**到期整册彻底抹除**（§1 清册制注、§4 清册行），README 只常驻章程 | 在位只进不改——留痕即记录；清册 = 合法彻底归零（bill 仪式 + 自足判据前置） |
（源：specs/README.md 目录表现行；本 delta 全文替换）

# 对 archive/README.md 的增删改（整篇重写）

### Requirement: archive/ 章程   <!-- 折叠时整文件替换 -->
# archive/ —— 已归档案卷（在位只进不改，到期整册归零）

> 身份：折叠完成案卷（`<YYYY-MM-slug>/` 整目录）的存放区与**工作清单**。本区是**可清册存储**：发布节点经清册 bill 将本体、下列清单、活面相关标识符**一并彻底抹除**（→ 归属法 §1「卷宗清册制」注、§4「卷宗清册执行」行）。本 README 本身常驻的是上方章程段；清单随清册归零，回到空壳态——**知识不以本区为存在依赖，因果全文住 `docs/explanation/`，条文现行文本住 `specs/current/`**。审计链不再是「本目录」：在位期内以本清单为工作表，清册后不留账（法不考古，git 历史非知识居住位）。

| 案卷（date-slug） | 主题一句话 | 状态 |
|---|---|---|
| （清册后本表为空壳，仅余表头；当前在库九案逐行登记，格式照旧——首期清册时随本体归零） | | |

在位纪律：三件套一字不回改（含错字；pg-native-shape 之折叠漏勾即按此不回改，留待首期清册再核表一笔处置）；推翻旧案 = 新立案，旧案即历史；拆件删、择留删皆违规，只认整袋清册。
> 案卷内出现的 `capabilities/` 系起草时旧目录名（2026-09-06 已更名 `current/`），在位记录不回改、清册后无记录。
（源：archive/README.md 现文 + archive 九案目录 2026-09-09 普查；本 delta 全文替换）

# 对 sample-application/specs/README.md 的增删改（镜像区）

### Requirement: 目录表 `archive/` 行   <!-- 整行替换 -->
| `archive/` | **已归档**：折叠完成的案卷——过程件；到期整册彻底抹除（框架法 §1 清册制注、§4 清册行，同文援引） | 在位只进不改；清册 = 合法归零，本区 README 只常驻章程 |
（源：sample-application/specs/README.md 现行表；本 delta 全文替换）
