# Spec-delta：2026-09-howto-codification

## ADDED（`specs/current/patterns/`）

`write-chain` `read-chain` `cross-aggregate` `application-objects` `domain-policy` `external-gateway` `aggregate-blueprint` `batch-write` `scheduler` `distributed-tx` `optimistic-lock` `testing-conformance` —— 12 卷位（11 新卷 + blueprint 承接清单），条款源 = 各 how-to 规范句入法，零新增规范。

## MODIFIED

- `specs/current/` 目录分架：`modules/`（8 卷）+ `patterns/`（11 卷）
- `modules/exception.md`：并入 error-handling 全链路条款（EV-x）
- docs 侧 13 篇 how-to → 宽松件（卷首法卷指针；new-aggregate 清单/验收整节迁出留指针）

## REMOVED

- 无（法条零废弃；仅容器迁移）
