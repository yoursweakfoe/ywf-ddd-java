# archive/ —— 已归档案卷（只进不改）

只进不改：完成折叠的 `<YYYY-MM-slug>/` 整目录移入此处。审计链 = git 历史 + 本目录（PEP「revision history is the historical record」判据）。

> 案卷内出现的 `capabilities/` 系起草时的旧目录名（2026-09-06 已更名 `current/`），历史记录不回改。
