<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.yoursweakfoe</groupId>
        <artifactId>ywf-ddd-common</artifactId>
        <version>0.0.1-SNAPSHOT</version>
    </parent>

    <artifactId>common-cloud</artifactId>
    <description>微服务治理聚合包（Nacos + Seata + Spring Cloud Feign/LoadBalancer/CircuitBreaker）</description>

    <dependencies>
        <!-- 服务注册发现（提供 DiscoveryClient，供 LoadBalancer 取实例）—— opt-in：消费方按需显式声明 -->
        <dependency>
            <groupId>com.alibaba.cloud</groupId>
            <artifactId>spring-cloud-starter-alibaba-nacos-discovery</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- 配置中心（spring.config.import=nacos: 声明式接入）—— opt-in：消费方按需显式声明 -->
        <dependency>
            <groupId>com.alibaba.cloud</groupId>
            <artifactId>spring-cloud-starter-alibaba-nacos-config</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- 分布式事务（AT/TCC）—— opt-in：消费方按需显式声明 -->
        <dependency>
            <groupId>org.apache.seata</groupId>
            <artifactId>seata-spring-boot-starter</artifactId>
            <optional>true</optional>
            <exclusions>
                <!-- 项目不需要 fastjson（SCA BOM 激活了 seata-all 的该依赖） -->
                <exclusion>
                    <groupId>com.alibaba</groupId>
                    <artifactId>fastjson</artifactId>
                </exclusion>
                <!-- 项目用 HikariCP，排除 druid -->
                <exclusion>
                    <groupId>com.alibaba</groupId>
                    <artifactId>druid</artifactId>
                </exclusion>
                <!-- 不走 Dubbo RPC -->
                <exclusion>
                    <groupId>org.apache.dubbo.extensions</groupId>
                    <artifactId>dubbo-filter-seata</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <!-- 声明式 HTTP 客户端 —— opt-in：本模块 2 个类仅依赖 feign 接口，消费方按需显式声明 -->
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-openfeign</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- 零信任身份（东西向 JWT 传播：Feign RequestInterceptor 读取已验签 Jwt 透传）—— opt-in：消费方按需显式声明 -->
        <dependency>
            <groupId>com.yoursweakfoe</groupId>
            <artifactId>common-security</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- 客户端负载均衡 —— opt-in：消费方按需显式声明 -->
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-loadbalancer</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- 熔断 / 降级（Resilience4J，不用 Sentinel）—— opt-in：消费方按需显式声明 -->
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-circuitbreaker-resilience4j</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- ========== 测试 ========== -->
        <dependency>
            <groupId>com.yoursweakfoe</groupId>
            <artifactId>common-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

</project>
