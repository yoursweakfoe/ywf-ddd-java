# <Agg> 行为契约（现行本）

<!-- 模板：业务现行册（镜像区）。复制本文件到 sample-application/specs/current/<agg>.md 后填写，交付前删注释。
     适用：示例业务一个聚合一份行为契约。这里写"业务承诺什么行为"，不写代码形状——形状权威在伞内法卷。 -->

> **身份**：<agg> 聚合的权威行为描述，现行本。本区管业务行为法；框架形状与横切规范见伞内 `knowledge/specs/`，冲突各管各层。代码行为与本文不符 = 修代码或走本区 `changes/` 修法。
> **机器对账**：<相关闸/C4 辖域说明：本区在伞扫描面外，业务名在本树合法。>

## 1. 状态机

<状态一句话清单。>

```
<状态迁移图（ASCII 或 mermaid）>
```

| 动作 | 前态 → 后态 | 守卫 | 违例错误码 |
|---|---|---|---|
| <place> | （无）→ <PENDING> | <命令校验> | <agg>:err.<scene> |

## 2. 全局门面（校验层分界）

<!-- 所有动作共用的校验（非状态机守卫），逐条编号 F-n：是什么、谁执行、错误码。 -->

| # | 校验 | 执行位点 | 违例 |
|---|------|---------|------|
| F-1 | <…> | <…> | <…> |

## 3. 写用例

<!-- 每个写动作一节。标题固定格式：### 3.n 动作名（XW-n） + HTTP 签名 → 响应。正文 = 业务前置、后置效果、错误码位。守卫句用业务语言，不写实现。 -->

### 3.1 <动作>（XW-1）　`POST /api/<resource>`　<参数摘要> → 200 `<Agg>CO`

- 前置：<…>
- 效果：<…>
- 违例：`<agg>:err.<scene>` → 422

## 4. 读用例

<!-- 同上：### 4.n 名称（RW-n） + HTTP 签名。写清过滤/排序/分页参数语义与空结果行为。 -->

### 4.1 详情　`GET /api/<resource>/{<id>}` → 200 `<Agg>CO`

## 5. 不变量

<!-- 跨状态恒真的业务事实，逐条 I-n；有机器背书的挂测试名。 -->

| # | 不变量 | 背书 |
|---|--------|------|

## 6. 错误码位总表（本聚合登记键）

<!-- 本聚合全部 {aggregate}:err.{scene} 键的镜像表；权威登记簿在伞内 how-to/error-handling.md，两处同 PR 同步。本聚合无键时保留本节，正文写"本聚合无登记键"。 -->

| 键 | 场景 | 触发用例 |
|----|------|---------|

---

**交稿自查**：

- [ ] 每个迁移/门面/不变量有编号；违例都有错误码位
- [ ] 错误码总表与伞内登记簿逐键对账
- [ ] 全文只写行为承诺，无代码形状（形状句 = 越界，移伞内法卷）
- [ ] 新用例先在本区 `changes/` 立四件套，批准施工后折叠进本册
